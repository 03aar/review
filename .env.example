# Database — SQLite file path (local) or Turso URL (production)
# Local:      DATABASE_URL=file:./reviewforge.db
# Production: DATABASE_URL=libsql://your-db-name.turso.io
DATABASE_URL=file:./reviewforge.db

# Turso auth token — required when DATABASE_URL is a Turso remote URL
# Get yours: turso db tokens create <db-name>
# TURSO_AUTH_TOKEN=

# Better Auth — generate a real secret: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-here-change-in-production

# App URLs
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000
